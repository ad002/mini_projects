#Take user input and split into single letters (split returns a list) 
x=input("Gimme some text to check whether palindrome \n > ")

#https://www.geeksforgeeks.org/reverse-string-python-5-different-ways/

#Solution 1: Using loops 
#def reverse_1(x):
    #Taking string as input 
#    str=''
    #until we haven't reached the end of the string
#    for i in x:
        #put in every ith element of x in str
#        str=i+str
#    return str
    

#print(revString(x))


#x = x[::-1] 

#Change order / Reverse string 
#for i in range(len(x)):
    #first item = last item (-1 because of 0 index)-counter variable
#    x[i] = x[(len(x-1))-i]	
    
#print(x)

	
